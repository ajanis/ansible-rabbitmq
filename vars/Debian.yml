rabbitmq_deps_packages:
  - apt-transport-https

local_rabbitmq_pkgs:
  - esl-erlang_{{ erlang_version }}_ubuntu_xenial_amd64.deb
  - rabbitmq-server_{{ rabbitmq_version }}_all.deb

rabbitmq_packages:
  - rabbitmq-server